server:
  port: 8721
spring:
  application:
    name: server-user-a
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-server
        profile: dev
        label: master
eureka:
  client:
    service-url:
      defaultZone: http://discovery-eureka-master:8761/eureka/,http://discovery-eureka-slaveone:8761/eureka/